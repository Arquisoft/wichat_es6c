
{
    "country": {  
         "query": "SELECT ?city ?cityLabel ?country ?countryLabel ?image WHERE {    ?city wdt:P31 wd:Q515.    ?city wdt:P17 ?country.   OPTIONAL { ?city wdt:P18 ?image. } SERVICE wikibase:label {  bd:serviceParam wikibase:language \"[AUTO_LANGUAGE],es\".   } }ORDER BY RAND() LIMIT ?limit",
         "question":"¿A qué país pertenece esta imagen?"
    }
}
